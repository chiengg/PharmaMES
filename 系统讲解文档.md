# 化工行业 MES 管理系统 · 系统讲解文档（对外演示版）

> 用途：对外演示时的系统讲解提纲，配合已部署的在线系统使用。
> 形态：纯前端静态高保真原型（HTML / CSS / 原生 JS，无后端依赖），可直接部署为静态站点。

## 一、系统定位与价值

- 面向**精细化工 / 制药行业**的制造执行系统（MES）高保真原型。
- 以"**动作管理为核心、变更管理为保障**"，将安质环管控从"离线 / 事后"转为"在线 / 事中"。
- 打通"**计划 → 准备 → 工单 → 执行 → 批次 → 放行 → 电子批记录**"全链路，覆盖生产、质量、物料、设备、合规、数据六大域。

## 二、功能全景（6 大业务域 / 35 个核心页面）

| 业务域 | 代表页面 |
| --- | --- |
| 工作台 | 生产驾驶舱、决策分析、我的待办 |
| 生产管理 | 生产计划、生产准备、生产工单、生产执行管理、工序动作管理、釜次BOM管理 |
| 安质环管控 | 实时预警、质量控制、清场放行、偏差CAPA、批次追溯、变更管理 |
| 物料 & 设备 | 称量配料、物料主数据、维护校准、设备 / 物耗能耗报表 |
| 主数据 & 合规 | 工艺配方、标准动作库、审计追踪、电子批记录归档 |
| 基础配置 | 组织工厂管理、用户与权限设置、AI投料推荐、系统集成联调、PDA现场采集 |
| 数据分析 | 产能釜次统计、设备管理、质量偏差追溯、生产计划执行、库存分析等报表 |

## 三、端到端业务流程（数据流转）

```
计划(PLAN) → 生产准备(PB) → 生产工单(WO) → 生产执行(釜次/批次 RC)
           → 质量控制(QC) → 清场放行(RE) → 批次追溯 → 电子批记录归档(eBR)
```

- **单号统一贯通**：采用 `YYMM + 业务前缀` 规则（WO-2608-xxx、RC-2608-xxx、RE-2608-xxx、DR-2608-xxx、CAPA-2608-xxx、CHG-2608-xxx），跨页可回链，异常（偏差 / 变更 / 预警）与主线实体强关联。
- **关键闭环示例**：阿莫西林 计划 `PLAN-2608-002` → 工单 `WO-2608-001` → 批次 `RC-2608-001` → 质控 → 放行 `RE-2608-xxx` → 批次追溯 → eBR 归档，全程可穿透。

## 四、核心亮点（演示重点）

### 4.1 多工厂数据隔离（组织 × 角色 双层权限）
- 顶部"工厂切换器"支持 **全厂视角 / 上海一厂 / 上海二厂 / 广州厂** 切换。
- 容器级 + 行级隔离：非本厂业务数据自动隐藏并展示"数据已按组织隔离"提示，支持申请跨厂授权。
- 配合字段级权限（见 4.2），构成"组织 × 角色"双层权限体系。

### 4.2 字段级权限（Q-07）
- 四级角色：操作员 / 车间管理者 / 工厂管理者 / 集团管理员。
- 字段可见性与操作级拦截（如偏差根因调查仅车间管理者以上可见，审批操作按角色拦截）。

### 4.3 AI 投料推荐（Q-03）
- 输入 产品 / 批次量 / 设备 / 水分 / 收率 / 校准漂移 / 湿度，AI 引擎按 BOM 基准 × 批次缩放 × 多补偿因子计算每物料推荐量与公差带、置信度、风险提示。
- "应用推荐到配料单"写入跨页共享状态，称量配料页顶部展示已应用的 AI 基准。

### 4.4 PDA 现场采集 / 离线续传（Q-01 / Q-04）
- 手机端原型：工序步骤条（扫码开工 → 称量录入 → 双人复核 → 报工确认）、称量口差自动判定、双人电子签名（ALCOA+）。
- 断网续传：监听 online / offline，指数退避重试 + 冲突裁决 + 同步日志。

### 4.5 eBR 电子批记录归档（Q-05）
- 以商业批次为主键，聚合 7 阶段全链路电子批记录：信息卡 + 合规 banner + 竖向时间轴 + 电子签名汇总（21 CFR Part 11）+ ALCOA+ 数据完整性 + 审计追踪。

### 4.6 系统集成联调（Q-06 / FR-INT）
- LIMS（质检结果拉取 → 生成放行号）、ERP（工单拉取）、WMS（发料 / 入库）三类活动联调，含 REST 契约样例 + 模拟异步调用 + 联调日志（计数 / 成功率 / 耗时）。

### 4.7 实时预警与异常闭环
- 预警 → 工单 / 批次 双向强关联；偏差CAPA、变更管理、预警处置形成异常闭环。

## 五、演示路径建议（约 10 分钟）

1. 打开系统门户（index.html），右上角切换"上海一厂"。
2. **生产计划** → 查看计划与排产；进入 **生产准备** 查看 5M1E 齐套。
3. **生产工单** → 点击工单查看明细；**生产执行管理** → 釜次 / 批次看板。
4. **批次追溯** → 按单号穿透到 **eBR 归档**，展示全链路电子批记录。
5. 亮点演示：**AI投料推荐**（生成配料基准）→ **PDA现场采集**（离线续传）→ **多工厂隔离切换**（切广州厂看隔离态）→ **字段级权限**（切角色看字段差异）。
6. **系统集成联调**：演示 LIMS / ERP / WMS 联调日志。

## 六、部署与访问

- 本仓库即系统源码，部署后通过 GitHub Pages 对外访问：
  **https://chiengg.github.io/PharmaMES/**
- 纯静态站点，无需后端；中文页面文件名由浏览器自动编码，可直接访问。

## 七、边界说明

- 本系统为高保真交互原型，数据均为示例数据；接口类需求以模拟调用 + REST 契约方式演示。
- 适合需求评审、客户演示、交互验证；正式上线需对接真实 ERP / SCADA / LIMS 与权限中心。
